# HIST-11: Remover Código Legado e Arquivos Obsoletos

## Contexto

Após as histórias anteriores, vários arquivos e construções ficam obsoletos: `EcuConnectionThread`, `EcuConnectionSerial` (substituído por `EcuSession + SerialTransport`), `EcuConnectionMock` (substituído por `MockEcuTransport`), `StateProcessorRegister`, `app/state/event.py` (EventType/VehicleStateChangeEvent internos), `signal_processor.emitter` legado, `_VehicleStateEmitter` em `VehicleState`, e a subscrição de `ECU_COMMAND_REQUESTED` que não tem mais consumidores.

## História de Usuário

Como desenvolvedor, quero remover todos os arquivos e construções obsoletas que foram substituídas nas histórias anteriores, para que o codebase não carregue código morto e a arquitetura seja clara.

## Requisitos Funcionais

- RF01: Remover `app/ecu_connection/thread.py` (`EcuConnectionThread`).
- RF02: Remover `get_ecu_connection_thread()` de `app/ecu_connection/__init__.py`.
- RF03: Deprecar e/ou remover `app/ecu_connection/serial.py` (`EcuConnectionSerial`) — substituto é `EcuSession + SerialTransport`.
- RF04: Remover `app/ecu_connection/mock_log.py` (`EcuConnectionMock`) — substituto é `MockEcuTransport`.
- RF05: Remover `app/ecu_connection/ecu_connection.py` (`EcuConnection` abstract) se não há mais implementações que a usam. Manter se `EcuSession` ainda a implementa.
- RF06: Remover `app/state/event.py` (`EventType`, `VehicleStateChangeEvent`) após confirmar que nenhum arquivo os importa.
- RF07: Remover `app/state/register.py` (`StateProcessorRegister`) se existir.
- RF08: Remover `app/state/processors/base.py` se `LambdaLoopStateProcessor` não mais herda de `Processor`.
- RF09: Remover `_VehicleStateEmitter` e a propriedade `emitter` de `VehicleState` (após HIST-08 completar a migração de `VeCalibrationScreen`).
- RF10: Remover o método legado `process_line()` de `SignalProcessor` e o atributo `emitter = Signal(dict)`.
- RF11: Remover o método legado `write(line)` de `LogWriter`.
- RF12: Avaliar se `ECU_COMMAND_REQUESTED` e `EcuCommandRequestedEvent` ainda têm consumidores. Se não, remover de `AppEventType`, `app_events.py` e `bus.py`.
- RF13: Remover `app/ecu_connection/ecu_connection.py` se `EcuSession` não mais herda de `EcuConnection`.

## Requisitos Não Funcionais

- RNF01: Antes de remover qualquer arquivo, fazer busca por grep de todos os imports do arquivo no codebase.
- RNF02: Remover arquivos em ordem de dependência: primeiro os dependentes, depois as bases.

## Especificações Técnicas

### Ordem de remoção recomendada

1. `app/ecu_connection/thread.py` — sem dependentes após HIST-10.
2. `app/ecu_connection/mock_log.py` — sem dependentes após HIST-10.
3. `app/ecu_connection/serial.py` — sem dependentes após HIST-10 (substituído por EcuSession).
4. `app/state/register.py` — sem dependentes após HIST-09.
5. `app/state/processors/base.py` — sem dependentes após HIST-09.
6. `app/state/event.py` — verificar imports em `VeCalibrationScreen` (HIST-08 deve removê-los).
7. `_VehicleStateEmitter` e `emitter` de `VehicleState` (em `state.py`) — após confirmar que `vehicle_state.emitter` não é acessado.
8. `SignalProcessor.emitter` e `process_line()` — após confirmar que `LambdaLoopStateProcessor` usa o Bus.
9. `LogWriter.write()` — após confirmar que `main.py` não chama mais diretamente.
10. `ECU_COMMAND_REQUESTED` — após confirmar que `LambdaToggle` usa método direto da Session.
11. `app/ecu_connection/ecu_connection.py` — se `EcuSession` não herda mais de `EcuConnection`.

### Grep de verificação antes de remover

```bash
# Verificar uso de EcuConnectionThread
grep -r "EcuConnectionThread\|ecu_connection_thread\|get_ecu_connection_thread" app/ main.py

# Verificar uso de EcuConnectionMock
grep -r "EcuConnectionMock\|mock_log" app/ main.py

# Verificar uso de EcuConnectionSerial
grep -r "EcuConnectionSerial" app/ main.py

# Verificar uso de StateProcessorRegister
grep -r "StateProcessorRegister\|register\." app/state/

# Verificar uso de EventType (app/state/event.py)
grep -r "from app.state.event\|VehicleStateChangeEvent\|EventType" app/ main.py

# Verificar uso de vehicle_state.emitter
grep -r "vehicle_state\.emitter\|_VehicleStateEmitter" app/ main.py

# Verificar uso de signal_processor.emitter (legado)
grep -r "signal_processor\.emitter\|SignalProcessor.*emitter\|\.emitter\.connect\|emitter\.emit" app/ main.py

# Verificar uso de ECU_COMMAND_REQUESTED
grep -r "ECU_COMMAND_REQUESTED\|EcuCommandRequestedEvent" app/ main.py
```

### Limpeza de imports em `__init__.py`

```python
# app/ecu_connection/__init__.py — após limpeza
from app.ecu_connection.session import EcuSession

ecu_session: EcuSession

def register_ecu_session(instance: EcuSession) -> None:
    global ecu_session
    ecu_session = instance

def get_ecu_session() -> EcuSession:
    return ecu_session

# get_ecu_connection() pode ser mantido como alias por um ciclo de limpeza:
def get_ecu_connection() -> EcuSession:
    """Deprecated. Use get_ecu_session()."""
    return ecu_session
```

## Arquivos de Referência

- `app/ecu_connection/thread.py` — remover
- `app/ecu_connection/mock_log.py` — remover
- `app/ecu_connection/serial.py` — remover
- `app/ecu_connection/ecu_connection.py` — avaliar remoção
- `app/ecu_connection/__init__.py` — simplificar
- `app/state/event.py` — remover
- `app/state/register.py` — remover (se existir)
- `app/state/processors/base.py` — remover (se existir)
- `app/state/state.py` — remover `_VehicleStateEmitter` e `emitter`
- `app/masterinjection/signal_processor.py` — remover `emitter` legado e `process_line()`
- `app/log_writer/log_writer.py` — remover `write()` legado
- `app/event/app_events.py` — avaliar remoção de `ECU_COMMAND_REQUESTED`
- `app/event/bus.py` — remover entrada de `ecu_command_requested` se evento removido

## Tasks

- [ ] TASK-01: Grep e verificar ausência de dependências para cada arquivo a remover.
- [ ] TASK-02: Remover `app/ecu_connection/thread.py`.
- [ ] TASK-03: Remover `app/ecu_connection/mock_log.py`.
- [ ] TASK-04: Remover `app/ecu_connection/serial.py`.
- [ ] TASK-05: Remover `get_ecu_connection_thread()` de `__init__.py`.
- [ ] TASK-06: Remover `app/state/event.py` após confirmar ausência de imports.
- [ ] TASK-07: Remover `app/state/register.py` (se existir).
- [ ] TASK-08: Remover `app/state/processors/base.py` (se existir).
- [ ] TASK-09: Remover `_VehicleStateEmitter` e propriedade `emitter` de `VehicleState`.
- [ ] TASK-10: Remover `SignalProcessor.emitter` e `process_line()`.
- [ ] TASK-11: Remover `LogWriter.write()`.
- [ ] TASK-12: Avaliar e remover `ECU_COMMAND_REQUESTED` se sem consumidores.
- [ ] TASK-13: Verificar se `EcuConnection` abstrata ainda é necessária; remover se não.

## Dependências

- Depende de: HIST-10 (main.py final), HIST-08 (VeCalibrationScreen migrada), HIST-09 (LambdaLoopStateProcessor migrado)
- Bloqueado por: HIST-10, HIST-08, HIST-09
- Permite: — (história final de cleanup)

## Notas / Decisões Pendentes

- Após remoção de `app/state/event.py`, qualquer referência a `EventType` em `VeCalibrationScreen._on_vehicle_state_event` deve ser substituída por verificação do `change_type` de outra forma (comparar com strings ou criar um enum novo em `app_events.py`). Esta verificação deve ser feita em HIST-08.
- `EcuConnection` abstrata pode ser mantida como documentação de interface, mesmo que `EcuSession` não herde dela formalmente.
