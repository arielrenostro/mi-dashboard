# História 12 — AppWindow, main.py e remoção de arquivos obsoletos

**ID:** 0015  
**Área:** main.py / Wiring  
**Fase:** 5

---

## Contexto

Esta é a história de fechamento da refatoração. Com todas as histórias anteriores implementadas, `main.py` precisa ser reescrito para usar a nova arquitetura (`EcuSession` + transports), e `AppWindow` precisa ter o parâmetro `signal_processor` removido do construtor. Também é o momento de remover os arquivos obsoletos: `serial.py`, `mock_log.py` e `thread.py`.

---

## História de Usuário

Como desenvolvedor, quero que `main.py` reflita a arquitetura final com `EcuSession`, sem conexões diretas via `emitter`, e sem arquivos de código legados, para que o codebase esteja limpo e alinhado com a especificação.

---

## Requisitos Funcionais

- `main.py` deve seguir exatamente a estrutura especificada na seção 10 da spec.
- `AppWindow` não deve receber `signal_processor` como parâmetro.
- Os arquivos `app/ecu_connection/serial.py`, `app/ecu_connection/mock_log.py` e `app/ecu_connection/thread.py` devem ser removidos.
- `app/ecu_connection/__init__.py` deve exportar apenas `register_ecu_session`, `get_ecu_session` (e opcionalmente `get_ecu_connection` como alias se ainda houver referências).
- Todos os componentes devem se inicializar corretamente na ordem especificada.
- O `EventMarker` e `LambdaToggle`/`KeyHoldDetector` devem ser comentados conforme especificação (desativados intencionalmente).

---

## Requisitos Não-Funcionais

- Ordem de inicialização importa: `QApplication` antes de qualquer `QObject`; `EcuSession.start()` por último antes de `app.exec()`.
- Nenhum arquivo legado deve ser importado em nenhum módulo após esta história.
- `get_ecu_connection()` como alias pode ser mantido temporariamente em `__init__.py` se outros módulos ainda o usarem — verificar todos os usos antes de remover.

---

## Especificação Técnica

### `main.py` — versão final:

```python
import logging
import os
import sys
from datetime import datetime

from PyQt6.QtWidgets import QApplication

from app.alarm.processor import AlarmProcessor
from app.config import config
from app.ecu_connection import register_ecu_session
from app.ecu_connection.serial_transport import SerialTransport
from app.ecu_connection.mock_transport import MockTransport
from app.ecu_connection.session import EcuSession
from app.event.app_events import AppEventType
from app.event.bus import event_bus
from app.log_writer.log_writer import LogWriter
from app.logger import setup_logging
from app.masterinjection.signal_processor import SignalProcessor
from app.state.state import vehicle_state
from app.ui.window import AppWindow

logger = logging.getLogger(__name__)


def _get_log_file_path() -> str:
    return os.path.join(config.datalog.path, f'log_stream_{datetime.now().strftime("%Y%m%d_%H%M%S")}.csv')


def main():
    setup_logging()
    logger.info("Starting app")

    app = QApplication(sys.argv)

    # Transport e Session
    if config.connection.mock != '':
        transport = MockTransport(config.connection.mock)
    else:
        transport = SerialTransport(config.connection.port, config.connection.baudrate)
    ecu_session = EcuSession(transport)
    register_ecu_session(ecu_session)

    # Processadores de dados (inscrevem no bus no próprio __init__)
    signal_processor = SignalProcessor()
    log_writer = LogWriter(log_file=_get_log_file_path())
    alarm_processor = AlarmProcessor(config.alarm.sound)

    # Subscrições sem dono natural
    event_bus.subscribe(AppEventType.SIGNALS_RECEIVED, lambda e: vehicle_state.update(e.data))
    event_bus.subscribe(AppEventType.ECU_COMMAND_REQUESTED, lambda e: ecu_session.send_command(e.command, e.args))

    # Keyboard actions (desativadas intencionalmente — manter comentado)
    # key_hold_detector = KeyHoldDetector(Qt.Key.Key_Space, hold_ms=2000)
    # lambda_toggle = LambdaToggle(config.ve_calibration)
    # event_marker = EventMarker(config.alarm.sound)
    # app_window.key_event.connect(key_hold_detector.on_key_pressed)
    # app_window.key_released.connect(key_hold_detector.on_key_released)
    # key_hold_detector.triggered.connect(lambda_toggle.handle_trigger)

    # UI
    app_window = AppWindow()   # sem parâmetro signal_processor
    app_window.show()

    # Iniciar comunicação (por último)
    ecu_session.start()

    app.exec()

    app_window.close()
    alarm_processor.stop()
    ecu_session.stop()


if __name__ == "__main__":
    main()
```

### `app/ui/window.py` — remover parâmetro `signal_processor`:

```python
# De:
class AppWindow(QWidget):
    def __init__(self, signal_processor: SignalProcessor):
        ...

# Para:
class AppWindow(QWidget):
    def __init__(self):
        ...
```

Verificar se `signal_processor` era usado em algum lugar dentro de `AppWindow` além do construtor — se sim, remover esses usos também.

### `app/ecu_connection/__init__.py` — versão final:

```python
from app.ecu_connection.session import EcuSession

_ecu_session: EcuSession | None = None


def register_ecu_session(session: EcuSession) -> None:
    global _ecu_session
    _ecu_session = session


def get_ecu_session() -> EcuSession:
    if _ecu_session is None:
        raise RuntimeError("EcuSession não registrada — chamar register_ecu_session() primeiro")
    return _ecu_session


# Alias para compatibilidade (remover após confirmar que nenhum módulo usa)
def get_ecu_connection() -> EcuSession:
    return get_ecu_session()
```

### Arquivos a remover:

```
app/ecu_connection/serial.py
app/ecu_connection/mock_log.py
app/ecu_connection/thread.py
```

**Procedimento de remoção:**
1. Verificar com `grep -r "from app.ecu_connection.serial import"` se há usos remanescentes.
2. Verificar com `grep -r "from app.ecu_connection.mock_log import"`.
3. Verificar com `grep -r "from app.ecu_connection.thread import"`.
4. Se não houver usos, deletar os arquivos.

---

## Arquivos de Referência

- `main.py` — versão atual a substituir
- `app/ui/window.py` — construtor atual com parâmetro signal_processor
- `app/ecu_connection/__init__.py` — registry atual
- `docs/plans/0002-spec-revision.md` (seção 10, 12) — estrutura final de main.py e arquivos a remover

---

## Arquivos Impactados

- `main.py` — reescrita completa
- `app/ui/window.py` — remover parâmetro signal_processor
- `app/ecu_connection/__init__.py` — atualizar para EcuSession

---

## Arquivos a Remover

- `app/ecu_connection/serial.py`
- `app/ecu_connection/mock_log.py`
- `app/ecu_connection/thread.py`

---

## Tasks

1. Abrir `app/ui/window.py` e verificar como `signal_processor` é usado no construtor e no corpo da classe.
2. Remover parâmetro `signal_processor` do `AppWindow.__init__()` e qualquer referência interna.
3. Reescrever `main.py` conforme especificação acima, usando `EcuSession`, `SerialTransport`, `MockTransport`.
4. Verificar que nenhum import de `serial.py`, `mock_log.py` ou `thread.py` permanece em qualquer arquivo do projeto (usar grep).
5. Deletar `app/ecu_connection/serial.py`, `app/ecu_connection/mock_log.py`, `app/ecu_connection/thread.py`.
6. Atualizar `app/ecu_connection/__init__.py` para a versão final com `register_ecu_session` / `get_ecu_session`.
7. Inicializar o app e verificar que ele sobe sem erros.
8. Testar com modo mock: verificar que sinais chegam, alarmes tocam, tabela VE popula.
9. Verificar que o log CSV está sendo gerado.

---

## Dependências

- **0010** (História 07 — SignalProcessor) — `process_line` e `emitter` removidos
- **0011** (História 08 — LogWriter) — `write()` removido, `emitter` desconectado
- **0012** (História 09 — AlarmProcessor) — `alarm_processor.start()` removido
- **0013** (História 10 — VehicleState) — `vehicle_state.emitter` removido
- **0014** (História 11 — VeCalibration UI) — `get_ecu_connection()` removido das telas
- **0009** (História 06 — EcuSession) — nova camada de comunicação disponível

---

## Pode ser paralelizado com

- 0014 (História 11) pode correr em paralelo se os dois desenvolvedores não tocarem em `main.py` simultaneamente.
