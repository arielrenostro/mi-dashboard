# História 10 — VehicleState desacoplado do emitter próprio

**ID:** 0013  
**Área:** VehicleState  
**Fase:** 4

---

## Contexto

O `VehicleState` atual tem um `_VehicleStateEmitter` (QObject com pyqtSignal) que usa `.emit()` dentro de `set_rpm_breakpoints`, `set_map_breakpoints` e `set_ve_map`. Isso cria um acoplamento entre o estado do veículo e o Qt, além de um padrão diferente dos demais componentes que usam o bus central.

A arquitetura alvo remove `_VehicleStateEmitter` e substitui as chamadas `.emit()` por `event_bus.publish()` via import local (para evitar import circular). Adicionalmente, `set_alarm` passa a receber `duration_s` configurável e a estrutura interna de alarmes muda para `(fired_at, expires_at)`.

---

## História de Usuário

Como desenvolvedor, quero que o VehicleState publique mudanças de estado via EventBus central ao invés de um emitter próprio, para que todos os componentes interessados possam assinar via bus sem acoplamento direto ao VehicleState.

---

## Requisitos Funcionais

- Remover `_VehicleStateEmitter` e a property `emitter`.
- `set_rpm_breakpoints()` deve publicar `VehicleStateChangedEvent(change_type=EventType.RPM_BREAKPOINTS, args=(breakpoints,))` via `event_bus` com import local.
- `set_map_breakpoints()` deve publicar `VehicleStateChangedEvent(change_type=EventType.MAP_BREAKPOINTS, args=(breakpoints,))`.
- `set_ve_map()` deve publicar `VehicleStateChangedEvent(change_type=EventType.FUEL_MAP, args=(ve_idx, ve_line))`.
- `set_alarm(signal, active, duration_s: float = 2.0)` passa a receber `duration_s` configurável.
- `_alarm_timestamps: dict[Signal, tuple[float, float]]` onde `(fired_at, expires_at)` substitui `dict[Signal, float]`.
- `is_alarm_firing(signal)` usa `expires_at` ao invés de `fired_at + ALARM_DURATION`.
- `is_any_alarm_firing()` usa `expires_at` ao invés de `fired_at + ALARM_DURATION`.
- Remover constante `ALARM_DURATION = 2`.

---

## Requisitos Não-Funcionais

- Import circular resolvido com import local dentro dos métodos `set_*`.
- Thread safety mantida com `_lock: threading.RLock` (sem alteração).
- Nenhum outro módulo deve importar `_VehicleStateEmitter` — verificar se há usos externos.

---

## Especificação Técnica

### `app/state/state.py` — versão alvo (partes relevantes):

```python
import threading
import time

from app.masterinjection.signal import ParsedSignal
from app.state.event import VehicleStateChangeEvent, EventType


class VehicleState:

    def __init__(self):
        self._lock = threading.RLock()
        self._signals: dict = {}
        self._alarm_timestamps: dict = {}  # Signal → (fired_at: float, expires_at: float)
        self._lambda_loop_closed: bool = False
        self._rpm_breakpoints: list[int] = [0 for _ in range(16)]
        self._map_breakpoints: list[int] = [0 for _ in range(16)]
        self._ve_map: list[list[int]] = [[0 for _ in range(16)] for _ in range(16)]

    # property emitter REMOVIDA

    def set_alarm(self, signal, active: bool, duration_s: float = 2.0) -> None:
        with self._lock:
            if active:
                now = time.time()
                expires_at = now + duration_s
                self._alarm_timestamps[signal] = (now, expires_at)
            else:
                self._alarm_timestamps.pop(signal, None)

    def is_alarm_firing(self, signal) -> bool:
        with self._lock:
            entry = self._alarm_timestamps.get(signal)
            if entry is None:
                return False
            _, expires_at = entry
            return time.time() < expires_at

    def is_any_alarm_firing(self) -> bool:
        with self._lock:
            now = time.time()
            return any(now < expires_at for _, expires_at in self._alarm_timestamps.values())

    def set_rpm_breakpoints(self, breakpoints: list[int]) -> None:
        with self._lock:
            self._rpm_breakpoints = breakpoints
        from app.event.bus import event_bus
        from app.event.app_events import VehicleStateChangedEvent
        event_bus.publish(VehicleStateChangedEvent(change_type=EventType.RPM_BREAKPOINTS, args=(breakpoints,)))

    def set_map_breakpoints(self, breakpoints: list[int]) -> None:
        with self._lock:
            self._map_breakpoints = breakpoints
        from app.event.bus import event_bus
        from app.event.app_events import VehicleStateChangedEvent
        event_bus.publish(VehicleStateChangedEvent(change_type=EventType.MAP_BREAKPOINTS, args=(breakpoints,)))

    def set_ve_map(self, ve_line: list[int], ve_idx: int) -> None:
        if len(ve_line) != 16:
            return
        if 0 > ve_idx > 15:
            return
        with self._lock:
            self._ve_map[ve_idx] = ve_line
        from app.event.bus import event_bus
        from app.event.app_events import VehicleStateChangedEvent
        event_bus.publish(VehicleStateChangedEvent(change_type=EventType.FUEL_MAP, args=(ve_idx, ve_line)))
```

### Impacto em `AlarmProcessor`

`process_signals()` deve passar `duration_s` para `vehicle_state.set_alarm()`:
```python
vehicle_state.set_alarm(signal, in_alarm, duration_s=duration)
```

### Impacto em `VeCalibrationScreen`

Remover do `__init__`:
```python
vehicle_state.emitter.connect(self._on_vehicle_state_event)
```
Adicionar em `on_activated()`:
```python
self._subscribe(AppEventType.VEHICLE_STATE_CHANGED, self._on_vehicle_state_event)
```
(Esta migração é coberta pela História 11.)

---

## Arquivos de Referência

- `app/state/state.py` — implementação atual a modificar
- `app/state/event.py` — VehicleStateChangeEvent e EventType (manter sem alteração)
- `app/alarm/processor.py` — usa vehicle_state.set_alarm() (precisa passar duration_s)
- `app/ui/ve_calibration/screen.py` — usa vehicle_state.emitter (migrado na História 11)
- `docs/plans/0002-spec-revision.md` (seções 7.1, 7.2) — especificação alvo

---

## Arquivos Impactados

- `app/state/state.py` — modificações conforme acima
- `app/alarm/processor.py` — passar `duration_s` em `set_alarm()`

---

## Novos Arquivos

Nenhum.

---

## Tasks

1. Abrir `app/state/state.py` e remover a classe `_VehicleStateEmitter` e a property `emitter`.
2. Remover a constante `ALARM_DURATION = 2`.
3. Alterar `_alarm_timestamps` para `dict[Signal, tuple[float, float]]`.
4. Reescrever `set_alarm(signal, active, duration_s=2.0)` usando `(now, expires_at)`.
5. Reescrever `is_alarm_firing(signal)` usando `expires_at`.
6. Reescrever `is_any_alarm_firing()` usando `expires_at`.
7. Reescrever `set_rpm_breakpoints()`, `set_map_breakpoints()`, `set_ve_map()` com `event_bus.publish()` via import local.
8. Verificar que a importação de `VehicleStateChangedEvent` funciona dentro do método (import local).
9. Atualizar `app/alarm/processor.py`: passar `duration_s=duration` em `vehicle_state.set_alarm(signal, in_alarm, duration_s=duration)`.
10. Verificar que `VeCalibrationScreen._on_vehicle_state_event` ainda funciona (recebe `VehicleStateChangedEvent` via bus após a migração da História 11).

---

## Dependências

- **0004** (História 01 — Novos Eventos no EventBus) — `VehicleStateChangedEvent` já existe, mas o bus precisa estar configurado para todos os tipos. Esta história usa `VEHICLE_STATE_CHANGED` que já existe — não depende tecnicamente da História 01, mas deve ser feita após Fase 1 para consistência.

---

## Pode ser paralelizado com

- 0010 (História 07 — SignalProcessor com buffers por frame)
- 0011 (História 08 — LogWriter com buffer D01+D02)
- 0012 (História 09 — AlarmProcessor QTimer)
